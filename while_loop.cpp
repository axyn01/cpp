#include <iostream>
using namespace std;
int main()
{
    int index = 0;
    while (index < 50)
    {
        cout << "we are at index number" << index << endl;
        index = index + 1;
    }
    return 0;
}

// the program will execute at least once and then it checks the condition.